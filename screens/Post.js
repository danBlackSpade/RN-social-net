import * as React from 'react';

import { View, Text } from 'react-native';

const Post = () => {

    return (
        <View>
            <Text>Post</Text>
        </View>
    )
};

export default React.memo(Post);

